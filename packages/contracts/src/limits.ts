/** Shared product limits; internal augmented matrices may have more columns. */
export const INPUT_LIMITS = Object.freeze({
  integerDigits: 32,
  decimalPlaces: 12,
  scalarCharacters: 128,
  dimension: 6,
  rationalDigits: 4096,
});
